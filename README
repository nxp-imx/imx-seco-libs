source code for iMX8 SHE/HSM drivers


How to build SHE/HSM libraries:
For linux OS:

	$ make

For QNX RTOS:
	$ export PATH=$PATH:<compiler path>
	$ export QNX_TARGET=<path to QNX SDP>/target/qnx7
	$ export QNX_HOST=<path to QNX SDP>/host/linux/x86_64
	$ export AR=ntoaarch64-ar
	$ export CC=ntoaarch64-gcc
	$ export TARGET_FLAVOR=qnx

	$ make

Note: /tmp folder is used for NVM storage by default, for using NVM storage please change path in "seco_os_abs_qnx.c" file.

For GHS OS:
	$ export PATH=$PATH:<compiler path>
	$ export GHS_BASE_BSP=<path to base BSP>
	$ export GHS_EXT_BSP=<path to extended BSP>
	$ export GHS_INC="-I ${GHS_BASE_BSP}/INTEGRITY-include "
	$ export GHS_INC+="-I ${GHS_EXT_BSP}/bspsrc/driver/crypto "
	$ export TARGET_FLAVOR=ghs
	$ export CC="ccarm64"
	$ export LD="cxarm64"
	$ export AR="cxarm64 -archive"
	$ export CFLAGS="-O1 ${GHS_INC} -gnu99"
	$ export ARFLAGS=""

	$ make
